# Simple React App

A minimal React starter app that renders in the browser with React 18.

## Getting started

Start a local static server from the project root:

```bash
npm start
```

Then open <http://localhost:4173> in your browser.
